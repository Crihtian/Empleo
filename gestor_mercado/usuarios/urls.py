from django.urls import path
# from .views import UserListView, UserUpdateView, UserDeleteView  # Comenta esta línea

urlpatterns = [
    # path('', UserListView.as_view(), name='user_list'),  # Comenta esta línea
    # path('<int:pk>/update/', UserUpdateView.as_view(), name='user_update'),  # Comenta esta línea
    # path('<int:pk>/delete/', UserDeleteView.as_view(), name='user_delete'),  # Comenta esta línea
]